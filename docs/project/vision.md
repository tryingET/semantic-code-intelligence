---
summary: "Project-local pointer to the canonical Semantic Code Intelligence vision."
read_when:
  - "You need the project vision from the docs/project read path."
  - "You were pointed at docs/project/vision.md by an operator or prompt."
type: "reference"
---

# Project vision

The canonical product vision currently lives at [`../../VISION.md`](../../VISION.md).

This project-local pointer exists so `docs/project/` read-order prompts can resolve the vision without duplicating the long-form source of truth. Keep substantive vision changes in the canonical root document unless the repo explicitly migrates that authority here.

For phased product posture and current implementation boundaries, read [`product-posture.md`](product-posture.md) next.
